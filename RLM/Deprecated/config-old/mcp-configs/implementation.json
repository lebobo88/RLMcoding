{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./"],
      "description": "File operations for code and tests"
    },
    "git": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-git"],
      "description": "Git operations for commits"
    }
  },
  "description": "Implementation agent configuration - filesystem + git",
  "tokenOverhead": "~4000 tokens",
  "useCase": "Code implementation, test writing, committing changes"
}

