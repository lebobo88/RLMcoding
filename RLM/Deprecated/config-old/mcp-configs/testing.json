{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "./"],
      "description": "File operations for reading source and writing tests"
    }
  },
  "description": "Testing agent configuration - filesystem only",
  "tokenOverhead": "~2000 tokens",
  "useCase": "Test writing, test analysis, coverage reports"
}

